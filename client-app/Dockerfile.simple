# 简化版前端 Dockerfile - 使用静态 HTML
# 如果 uni-app 构建失败，可以使用这个版本
FROM nginx:alpine

# 复制静态 HTML 文件
COPY public/index.html /usr/share/nginx/html/index.html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
